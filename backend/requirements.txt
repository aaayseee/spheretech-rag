
Backend requirements · TXT
# ── Core RAG pipeline ─────────────────────────────────────────────────────────
sentence-transformers>=2.2.0   # multilingual embeddings (paraphrase-multilingual-MiniLM-L12-v2)
faiss-cpu>=1.7.0               # vector similarity search index
numpy>=1.24.0                  # array operations for embeddings
pandas>=1.5.0                  # knowledge base CSV loading
 
# ── LLM API ───────────────────────────────────────────────────────────────────
requests>=2.28.0               # Groq API calls (sync + streaming)
 
# ── FastAPI backend (bonus phase) ─────────────────────────────────────────────
fastapi>=0.100.0               # REST API framework
uvicorn>=0.22.0                # ASGI server for FastAPI
 
# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0           # load GROQ_API_KEY from .env file