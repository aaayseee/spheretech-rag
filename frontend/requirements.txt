# ── UI ────────────────────────────────────────────────────────────────────────
streamlit>=1.35.0              # web UI framework

# ── Markdown rendering ────────────────────────────────────────────────────────
Markdown>=3.4.0                # convert LLM markdown output to HTML for display

# ── Backend deps (needed when running without docker-compose) ─────────────────
sentence-transformers>=2.2.0
faiss-cpu>=1.7.0
numpy>=1.24.0
pandas>=1.5.0
requests>=2.28.0
python-dotenv>=1.0.0